name: build and deploy

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      username: ${{ secrets.DOCKER_USERNAME }}
      password: ${{ secrets.DOCKER_PASSWORD }}
    steps:
      - uses: actions/checkout@v3

      - name: Build the Docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/hotel.api:latest .

      - name: Push the Docker image
        run: |
          docker login ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
          docker push ${{ secrets.DOCKER_USERNAME }}/hotel.api:latest

  deploy:
    runs-on: windows-2022
    needs: build

    env:
      private_key: ${{ secrets.PRIVATE_KEY }}
      username: ${{ secrets.DOCKER_USERNAME }}

    steps:
      - uses: action/checkout@v3

      - name: SSH to remote server
        run: |
          ssh -o StrictHostKeyChecking=no -i ${{ secrets.PRIVATE_KEY }} azureuser@20.239.179.81
          sudo docker service update --image ${{ secrets.DOCKER_USERNAME }}/hotel.api:latest hotelserver_rest_hotel
